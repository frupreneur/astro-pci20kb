---
import { footerMenu } from "../../db";

---

<footer>
  <div class="footer">
   
    <ul class="footer-menu">
      {footerMenu.map((menuItem, i) => (
        <li>
          <a
            href={menuItem.path}
            target={menuItem.name === "Site Map" ? "_blank" : ""}
          >
            {menuItem.name}
          </a>
        </li>
      ))}
    </ul>
    <p>
      {`© Photo Convert Into 20KB ${new Date().getFullYear()}`}{" "}
    </p>
  </div>
</footer>

